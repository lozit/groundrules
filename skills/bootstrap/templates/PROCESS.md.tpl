<!-- generated-by: groundrules v1.3.2 -->
# Process — {{PROJECT_NAME}}

> The **working method contract** for this project: how we work together, phase by phase, with explicit validation gates. This is neither the intent (`intake/`) nor the vision (`docs/VISION.md`) — it's the *how we proceed*. Claude must follow it and **never skip a phase**.

## Phases

<!-- Define your phases. Example shape (adapt freely — 3 phases or 10, your call):

- **Phase 1 — Functional scope**: interview → functional spec draft. No technology talk yet. GATE: user validates the spec.
- **Phase 2 — UX/UI rules**: colors, framework, typography. GATE: user validates the design system.
- **Phase 3 — Per page/feature**: spec → prototype (iterate) → data dictionary. GATE: each page validated individually.
- **Phase 4 — Final data model**. GATE: review.
- **Phase 5 — Stack**. GATE: review.
- **Phase 6 — Tests defined before building**. GATE: review.
- **Phase 7 — Backend**. / **Phase 8 — Frontend**.
-->

1. <fill in>

## Validation gates

- A phase is **done** only when explicitly validated by <who?>.
- Intermediate deliverables are marked `[~] in review` in `PLAN.md` until validated.

## Working style

- Interviews: grouped questions (3-4 at a time, with options) rather than one long questionnaire.
- <other preferences: prototype-first? mock one screen before propagating visual changes? plan mode by default?>

## Where artifacts live

- Per-phase working artifacts: <e.g. `docs/phases/<n>/` or alongside the feature>
- Synthesized, stable outcomes migrate to `docs/` (vision, architecture, data model…)
