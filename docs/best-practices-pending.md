<!-- generated-by: groundrules v1.7.0 -->
# Best practices pending — to review manually

File generated by `/starter-kit:apply-best-practices` on 2026-05-11. Contains the recommendations **not applied automatically** (hooks, custom skills) awaiting human review.

---

## ~~Hook: PreToolUse — unsubstituted `{{KEY}}` validation~~ — **dropped** (ADR 0025)

Rejected: a runtime hook goes against "template over code" / offline-first / harness-portability, and `verify-bootstrap` already catches leftover `{{KEY}}` post-hoc (robustly, with the backtick rule since 2026-06-08). See `docs/decisions/0025-no-runtime-hook-no-watch.md`.

---

## ~~Deferred skill: `/groundrules:verify-bootstrap`~~ — **implemented in V0.6** ✅

The skill is now available: `skills/verify-bootstrap/SKILL.md`. Run `/groundrules:verify-bootstrap` (report only) or `/groundrules:verify-bootstrap --fix` (fixes signature version mismatches). Also detects leftover `{{KEY}}` and validates JSON.

---

## ~~Deferred command: `/watch-bootstrap`~~ — **dropped** (ADR 0025)

Rejected: niche, no demand; the CLAUDE.md-size concern is served by `/groundrules:slim` + `verify-bootstrap` on demand. See `docs/decisions/0025-no-runtime-hook-no-watch.md`.

---

**Queue status (2026-06-08): empty** — all items resolved (verify-bootstrap implemented; hook + watch dropped, ADR 0025).

**How to use this file**: review each section, make a decision (implement / defer / drop), clean up this file as you go. If a practice is implemented, add an entry in `.groundrules.json` `appliedPractices` (status: `applied`).
